# Save a Liesel model to a pickle file

Save a Liesel model to a pickle file

## Usage

``` r
save_model(model, path)
```

## Arguments

- model:

  The model to save.

- path:

  The file path to save the model to.
